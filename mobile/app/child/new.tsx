import { ChildFormScreen } from '../../src/components/child-form-screen';

/** Add a child (§3). The form itself lives in `child-form-screen.tsx`,
    shared with the edit route. */
export default function NewChild() {
  return <ChildFormScreen />;
}
